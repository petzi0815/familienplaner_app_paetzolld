# @familienplaner/web

Next.js 16 App (UI + `/api/v1` + Worker) des Familienplaners. Teil des Monorepos —
siehe Root-[`README.md`](../../README.md) und [`CLAUDE.md`](../../CLAUDE.md).

```bash
npm install          # (im Repo-Root, npm workspaces)
npm run dev          # http://localhost:3000
```
