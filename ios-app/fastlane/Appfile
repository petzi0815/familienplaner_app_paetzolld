app_identifier(ENV["APP_BUNDLE_ID"] || "app.yagemi.familienplaner")
apple_id(ENV["APPLE_ID"])        # optional — API-Key ersetzt Login
itc_team_id(ENV["ASC_TEAM_ID"])  # optional — App Store Connect Team
team_id(ENV["APPLE_TEAM_ID"])    # Developer-Portal-Team-ID
